let phepTinh = prompt ("Chọn 1 trong 4 phép tính sau cho a và b: + - * /")
let a = prompt("Nhập số a:")
    let b = prompt ("Nhập số b:")
if (phepTinh = "+" ) {
    
    console.log("Kết quả:", parseIn(a) + parseIn(b));
}